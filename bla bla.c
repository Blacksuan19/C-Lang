#include <stdio.h>
int main(int argc, char const *argv[])
{
	int a, b, c;
	if (a > 0)
	{
		if (b > 0 )
		{
			printf("option one\n");
		}
		else if (c > 0)
		{
			printf("option one\n");
		}
	}
	else
		printf("option two\n");
	return 0;
}