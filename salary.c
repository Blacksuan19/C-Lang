// cant even remember what this was suppsed to do 
#include <stdio.h>
int main(int argc, char const *argv[])
{
	char state;
	int salary, years, total_salary;
	printf("enter status:\n");
	scanf("%s", &salary);
	if (salary == 'F')
	{
		printf("enter number of years of service:\n");
		scanf("%d", &years);
		if (years < 5)
		{
			printf("enter salary:\n");
			scanf("%d", &salary);
			total_salary = 
		}
	}
	return 0;
}