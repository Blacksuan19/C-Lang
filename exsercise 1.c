#include <stdio.h>
int main()
/*smh i made it kinda complicated */
/* writted on kate (brackets is having an IDE moment)*/
{	int x, y, p, s, total; 
	printf("Inter the value of X :");
	scanf("%d", &x);
    printf("the value of x is  %d\n", x);
    printf("Inter the value of y: ");
	scanf("%d", &y);
    printf("the value of y is: %d\n", y);
    p = x * x;
    s = y * y;
    total = p + s;
	printf("total is: %d\n", total);
	return 0;
} 
